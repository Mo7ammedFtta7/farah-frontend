import { JsonttransPipe } from './jsonttrans.pipe';

describe('JsonttransPipe', () => {
  it('create an instance', () => {
    const pipe = new JsonttransPipe();
    expect(pipe).toBeTruthy();
  });
});
